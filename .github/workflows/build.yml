name: 自动生成无验证IPA
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: 解压原始IPA
        run: |
          unzip -q original.ipa -d Unpacked
          APP_PATH=$(echo Unpacked/Payload/*.app)
          echo "APP路径: $APP_PATH"

      - name: 编译绕过代码
        run: |
          clang -arch arm64 \
            -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
            -framework UIKit -framework Foundation \
            -miphoneos-version-min=12.0 -fobjc-arc \
            -dynamiclib Bypass.m -o Bypass.dylib

      - name: 修改Info.plist
        run: |
          APP_PATH=$(echo Unpacked/Payload/*.app)
          cp Bypass.dylib $APP_PATH/
          
          # 先添加字段，再赋值
          /usr/libexec/PlistBuddy -c "Add :NSPrincipalClass string BypassEntry" $APP_PATH/Info.plist
          
          brew install ldid
          ldid -S $APP_PATH/Bypass.dylib

      - name: 重新打包IPA
        run: |
          cd Unpacked
          zip -qr ../IBOX-无验证版.ipa .

      - name: 上传成品
        uses: actions/upload-artifact@v4
        with:
          name: IBOX-无验证版
          path: IBOX-无验证版.ipa
